# ============================================
# DATABASE
# ============================================
# Copie do Supabase → Settings → Database → Connection String
# Use a URL com pooler (porta 6543) para production
DATABASE_URL="postgresql://postgres:SUA_SENHA@SEU_HOST.supabase.co:6543/postgres?pgbouncer=true"

# ============================================
# AUTHENTICATION
# ============================================
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="gere-uma-chave-secreta-aleatoria"  # openssl rand -base64 32

# ============================================
# PWA TRACKING
# ============================================
# Hash salt para anonimizar IPs (GERE UMA CHAVE ÚNICA!)
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
HASH_SALT="fc34ad09d93f921a989289ff5d97c6403623a6cdb90562b06d89a956ae7d8aca"

# URL pública do app (usado para gerar links de instalação)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ============================================
# EMAIL (OPCIONAL - Para envio de emails)
# ============================================
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="seu-email@gmail.com"
# SMTP_PASS="sua-senha-app"
# SMTP_FROM="Sushi World <noreply@sushiworld.com>"

# ============================================
# ANALYTICS (OPCIONAL)
# ============================================
# NEXT_PUBLIC_GA_ID="G-XXXXXXXXXX"
# NEXT_PUBLIC_FB_PIXEL_ID="123456789"

# ============================================
# INSTRUÇÕES
# ============================================
# 1. Copie este arquivo para .env.local
# 2. Substitua todos os valores de exemplo pelos reais
# 3. NUNCA commite .env.local no git!
# 4. Para produção (Vercel), configure em: Settings → Environment Variables
